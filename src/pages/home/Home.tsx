import React, { useState } from "react";
import "./Home.css";

const Home = (): JSX.Element => {
    return (
        <div className="home">
            <img src="/logo_full.png" className="w-100"></img>
        </div>
    );
};

export default Home;
