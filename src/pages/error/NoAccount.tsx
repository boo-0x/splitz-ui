import React from "react";
import "./NoExtension.css";

const NoAccount = (): JSX.Element => (
    <div className="no-extension">
        <div className="no-extension-header">
            <div>Create your Reef account</div>
        </div>

        <div className="no-extension-subheader">
            Use Reef Chain Extension to create your account and refresh the page.
        </div>
    </div>
);

export default NoAccount;
